# Legacy vanilla application

React 마이그레이션 이전의 HTML/JavaScript 구현을 rollback 참고용으로 보관한다.

- 실제 서비스 진입점은 루트 `index.html`과 `src/main.jsx`이다.
- `legacy/` 아래 파일은 빌드와 lint 대상이 아니다.
- React 기능 동등성 승인 후 별도 커밋에서 제거할 수 있다.
